import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adrianahyatt.sleepsocialtracker',
  appName: 'Sleep Social Tracker',
  webDir: 'dist'
};

export default config;
